ggplot(mass.for.model, aes(x=age, y=d.mass)) +
  geom_point() +
  theme_classic() +
  facet_wrap(~site)

plot(mass)

coef(summary(mass))
str(sum.mass.by.age)
sum.mass.by.age

summary(m.mass.by.age)$coefficients[2,4] %>% round(digits=2)
