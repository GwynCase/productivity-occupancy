ggplot(ml.tz, aes(x=amt.tz, y=ratio, label=site)) +
  geom_point() +
  geom_text(hjust='inward', nudge_y = 0.5) +
  theme_classic()


sq.vri %>% mutate(older=sum(mature, old))

summary(sq.x.mature)

summary(models)
