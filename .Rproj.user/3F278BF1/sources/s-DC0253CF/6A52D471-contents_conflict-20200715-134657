ggplot(ml.tz, aes(x=amt.tz, y=ratio, label=site)) +
  geom_point() +
  geom_text(hjust='inward', nudge_y = 0.5) +
  theme_classic()

levels(r.bec)

utz.loc <- cam.sites.sf %>% filter(site == 'UTZ')

show_cores(r.bec, class=c(1, 2), labels=FALSE)

show_patches(landscape, class = c(1, 2))

check_landscape(r.bec)
